import * as types from "../constants/clientActionType";

export const changerNomClient = (idClient, newName) => ({
  type: types.CHANGER_NOM_CLIENT,
  idClient,
  newName
});

export const cretors = payload => ({
    type: """""",
    payload
})*

this.props.actions.cretors({
    idClient, newName
})

let { idClient, newName } = action.payload;